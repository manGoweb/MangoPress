{initAdminComponent Shapes, [ data => [ url => "{$assetsPath}/sprites/{$buildstamp}shapes.svg" ] ]}

<div class="wrap admin-page-example">
	<h1>{$title}</h1>
	<p>Hello, your <code>$foo</code> is "{$foo}".</p>

	{shape symbol-plus}
</div>
